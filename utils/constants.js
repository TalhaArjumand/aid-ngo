/**
 * This enum handles the different statuses to be listened to while disabling certain campaign Features
 * @ param {array}
 *
 */

const statuses = ["active", "completed", "ongoing", "ended"];

export { statuses };
